interface:
  display_name: "Project Context"
  short_description: "Automatically maintain durable project context"
  default_prompt: "Use $project-context to retrieve and maintain durable intent for this repository task."
policy:
  allow_implicit_invocation: true
